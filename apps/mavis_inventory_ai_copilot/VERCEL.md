# Vercel deploys only the static landing page in public/.
# Set Root Directory in Vercel project settings to:
#   apps/mavis_inventory_ai_copilot
#
# The Streamlit app runs via streamlit_app.py (local, Docker, or Streamlit Cloud).
